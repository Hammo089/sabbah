# تحديث — نسخة كاملة

## ١. استبدل مجلد المشروع
احذف محتوى `~/Downloads/sabbah-platform` (ما عدا `.git` و `.env.local`) وفك الضغط مكانه.

## ٢. طبّق SQL بالترتيب
Supabase → SQL Editor:

1. `0009_licensing_and_theme.sql` — إذا ما طبّقته
2. `0010_submissions_and_chrome.sql` — إذا ما طبّقته
3. `0011_modules_and_season_details.sql`
4. `0012_backdrop_and_glass.sql` ← **الجديد**

## ٣. متغيّرات Vercel (النوع **Config** مش Secret)

| المفتاح | لشو | إلزامي |
|---|---|---|
| `ANTHROPIC_API_KEY` | تلخيص النصوص + الشات | لهالميزات |
| `AI_MODEL` | اسم الموديل بحسابك | اختياري |
| `B2B_ACCESS_SECRET` | توقيع كوكي دخول المشترين | **نعم** |
| `CRON_SECRET` | تذكير العقود (وبيصير احتياطي لـ B2B) | نعم |
| `SUBMISSION_IP_SALT` | تجزئة IP المقدِّم | مستحسن |
| `RESEND_API_KEY` + `LICENCE_ALERT_FROM` | إيميل تذكير العقود | اختياري |

## ٤. الرفع

```bash
cd ~/Downloads/sabbah-platform
npm install
npm run build
git add -A
git commit -m "feat: CAPDAMS module grid, tabbed season editor, account-free B2B, direct user creation"
git push
```

---

# شو صار بهالنسخة

## الخطوة ١ — الداشبورد صار متل CAPDAMS

الصفحة الرئيسية للداشبورد صارت شبكة موديولات مقسّمة ٥ مجموعات، كل موديول مربّع فيه أيقونة وعدد سجلاته الحيّ، وبادج أحمر ع اللي بدّو انتباه.

**اللي كان ناقصنا من CAPDAMS وانضاف:**

| الموديول | الحالة |
|---|---|
| Tags | جديد — وسوم متعدّدة اللغات بتنربط بأي عمل |
| Library | جديد — الماسترات والملفات لكل عمل (نوع، فورمات، دقّة، مدّة، حجم، مكان، باركود) |
| Master Scenes | جديد — سجل المشاهد بالتايم‌كود لكل حلقة |
| News & Press | جديد — أخبار ومقالات بالثلاث لغات مع نشر/مسوّدة |
| Social Media | جديد — جدول واحد بيغطّي Instagram و YouTube و Facebook و Twitter و TikTok و LinkedIn بدل ٤ جداول متشابهة |
| Exports | جديد — سجل كل تصدير + زرّ توليد الكتالوغ |
| Tracking | جديد — سجل تدقيق: مين غيّر شو وإيمتى |
| Notifications | جديد — تنبيهات، والعقود القريبة من الانتهاء محسوبة لحظياً مش مخزّنة |
| Website | جديد — كل صفحات الموقع بمكان واحد مع اللي بيتحكّم فيها |
| B2B Buyers | جديد — كل مين فتح الكتالوغ |

**اللي عنا وما كان بـ CAPDAMS:** Submissions، Appearance، Legacy، Broadcasters، Partners.

**Seasons** كان تبويب لحاله عندهم — عنا هو `/admin/series` (نفس الجدول، `kind` بيفرّق).

## الخطوة ٢ — إضافة المسلسل صارت متل الصورة

صفحة الموسم صار عندها نفس شريط التبويبات:
Summary · Details · Broadcast · Images · Posters · Gallery · Synopsis · Episodes · Watch · Cast · Crew · Website · Social Media · Tracking

وتبويب **Details** طبق الأصل:
- Region / ID / Country / SeasCode / ProgCode بالسطر الأول
- ٤ شبكات شيك‌بوكس: Genres (٣٦ تصنيف) · Audio · Dubbing · Subtitling
- تحت كل شبكة الصندوقين: إنكليزي وعربي

**فرق واحد مقصود:** الصندوقين محسوبين من الشيك‌بوكسات، مش بتكتبهن. بالنظام القديم كانوا حقول منفصلة — وهيك بيصير عمل مكتوب عليه "Drama, Social" بالإنكليزي و"دراما" بس بالعربي. هلق مستحيل يختلفوا.

كمان: التبويب المفتوح بينحفظ لكل سجل، فلما تحفظ ما بترجع ع Summary.

العمل الجديد بيفتح استمارة الأساسيات أول شي — لأنّه ما في سجل بعد لتعليق التبويبات عليه.

## الخطوة ٤ — التصليحات

**DRM ما كنت تقدر ترجع تفوت** — السبب: اللائحة كانت `useState(rows)` بتقرا القيمة مرّة وحدة بس. السيرفر كان يحفظ ويعمل revalidate صح، بس React بيضلّ ماسك اللقطة القديمة — فالرخصة اللي عملتها ما بتظهر أبداً وما فيك تفتحها. انصلح بمزامنة اللائحة مع السيرفر. ونفس العطل كان بشريط الأخبار — انصلح كمان.

**إضافة مستخدم بدون دعوة** — `/admin/users` صار فيه "إضافة مستخدم مباشرة": إيميل، اسم، صلاحية، وكلمة مرور مؤقّتة (أو بتتولّد لحالها). الحساب بينفتح فوراً بدون إيميل دعوة وبدون انتظار. كلمة المرور بتظهر مرّة وحدة مع زرّ نسخ — نحن ما بنخزّنها.

**B2B بدون تسجيل** — ما عاد في حساب ولا كلمة مرور ولا إيميل تأكيد. المشتري بيعبّي: الاسم، الشركة، **المنصب**، **رقم الهاتف** (والإيميل اختياري)، وبيفتح الكتالوغ. بينحفظ كـ lead بتشوفه بـ `/admin/leads` مع عدد مرّات التحميل، والوصول بيضلّ مفتوح ٣٠ يوم بكوكي موقّع.

## الحماية

- بيانات المشترين (رقم هاتف مباشر لرئيس مشتريات) صارت **admin فقط** — لقيت إنّ `is_staff()` بيشمل المحرّرين، فضيّقتها. مجرّب: المحرّر بيشوف `0`، الـ super_admin بيشوف الصفوف.
- سجل التدقيق: المحرّر بيقدر يضيف عليه بس ما بيقدر يقراه — سجل بيقدر أي حدا يعدّله مش سجل.
- مسار الكتابة الجديد للموديولات وراه allow-list: اسم الجدول لازم يكون بالقائمة، وكل حقل بيمرق ب Zod. مستحيل توجّهه ع `users_profiles` أو `drm_licenses`.
- كوكي الـ B2B ادّعاء موقّع بـ HMAC، مش سرّ حامل — بيفتح الكتالوغ العام وبس.

---

## الخطوة ٣ — الحركة (مرحلة ١ من ٢)

حللت الفيديو (٤١ فريم) ونقلت الحركات ع ألوان الصباح — أسود وأخضر، مش أبيض متل المرجع.

**اللي صار بهالمرحلة:**

| الحركة | وين |
|---|---|
| كشف بالسكرول (fade + rise) | كل الصفحات — عبر `PageShell` فبتشمل الجديد تلقائياً |
| كشف متتالي (stagger) | صفوف الكتالوغ، الشوكيس، ميزات B2B، الشركاء، بنود الشروط |
| ترانسيشن بين الصفحات | `template.tsx` — بينعمل remount مع كل تنقّل، فوق شاشة الشعار |
| كولاج طايف بالهيرو | بدل الشريط العمودي — كروت مايلة عم تطفو، كل وحدة ع دورة مختلفة |
| عدّاد بيعدّ | أرقام الإحصائيات لما توصلها بالسكرول |
| صفّين مارکي بعكس بعض | قسم جديد بالرئيسية — كروت مايلة مع زرّ تشغيل |

**كل الحركات المستمرّة ورا بوّابة وحدة** — `useMotionAllowed()`:

- `prefers-reduced-motion` → ما في ولا حركة مستمرّة
- توفير بيانات أو شبكة 2G → مطفي
- ٤ أنوية أو أقل، أو ٤ غيغا رام أو أقل → مطفي
- التبويب مخفي → بتتوقّف

الكشف بالسكرول رخيص فبيضلّ شغّال بكل الحالات ما عدا reduced-motion. وما في حركة إلا ع `transform` و`opacity` — الاتنين عالكرت الرسومي، فما في layout ولا paint.

الكولاج بيتحكّم فيه نفس مفتاح `hero_show_strip` بـ `/admin/appearance`.

**مرحلة ٢ (اللي جاي):** إعادة بناء أقسام الرئيسية متل الفيديو — القسم المقسوم بالأسهم المنقّطة، والمسار المتموّج بالكروت.


---

# فيديو الخلفية والتصميم الزجاجي (جديد)

## أول شي — حوّل الملف

ملفك `.mkv` وما بيشتغل بأي متصفّح. وبدّك **ملفّين مش واحد**:

```bash
cd ~/Desktop

# ١. الخلفية: ٣٠ ثانية، بدون صوت، مضغوط
ffmpeg -i 71th.mkv -t 30 -an \
  -vf "scale=1920:-2" -c:v libx264 -preset slow -crf 30 \
  -movflags +faststart 71th-bg.mp4

# ٢. نسخة WebM أصغر (اختياري، بتوفّر ~٣٠٪)
ffmpeg -i 71th-bg.mp4 -c:v libvpx-vp9 -crf 40 -b:v 0 -an 71th-bg.webm

# ٣. الصورة الثابتة — بتظهر عالموبايل وقبل ما يحمّل الفيديو
ffmpeg -i 71th.mkv -ss 3 -vframes 1 -vf "scale=1920:-2" 71th-poster.jpg

# ٤. الفيلم الكامل مع صوت
ffmpeg -i 71th.mkv -c:v libx264 -preset slow -crf 24 \
  -c:a aac -b:a 128k -movflags +faststart 71th-full.mp4

ls -lh 71th-*
```

`-movflags +faststart` ضروري — بدونه المتصفّح بيستنى الملف كلّه قبل ما يبلّش.

**تأكّد إنّ `71th-bg.mp4` أقل من ١٥ ميغا.** إذا أكبر، زيد `-crf` لـ 32 أو 34.

## بعدين — ارفعهم

Supabase → **Storage** → البكت اسمه `video` (الميغريشن عملها، عامّة وسقفها ٥٠٠ ميغا) → ارفع الأربع ملفات → من كل ملف انسخ **Get URL**.

## بعدين — الصقهم بالداشبورد

`/admin/appearance` → قسم **فيديو الخلفية**:

| الحقل | الملف |
|---|---|
| رابط اللوب (MP4) | `71th-bg.mp4` |
| رابط اللوب (WebM) | `71th-bg.webm` |
| رابط الصورة الثابتة | `71th-poster.jpg` |
| شغّل الفيديو خلف الموقع | ✅ |

وقسم **فيلم الـ ٧١**: رابط الفيلم الكامل = `71th-full.mp4`.
وقسم **الأسطح الزجاجية**: ✅.

ما بتحتاج deploy — كلّه من الداشبورد.

**لو الفيلم الكامل أكبر من ١٠٠ ميغا:** ارفعه ع يوتيوب "غير مدرج" بدل Supabase. الباندويدث ببلاش وبيشتغل ع كل شبكة.

## شو بيصير بالضبط

- الفيديو ثابت خلف الموقع كلّه (أو الرئيسية بس — انت بتحدّد)
- الترويسة والفوتر واللوح الرئيسي بكل صفحة صاروا زجاج مضبّب
- زرّ **٧١** الزجاجي بالواجهة: بتكبسه → الفيلم بيفتح ملء الشاشة مع صوت من البداية → **رجوع للموقع** بيرجّعك والخلفية بتكمّل صامتة

## الحمايات المبنيّة جوّا

| الحالة | شو بيصير |
|---|---|
| موبايل | **الصورة الثابتة، مش الفيديو** — إلّا إذا فعّلت المفتاح يدوياً |
| تقليل الحركة | الصورة الثابتة، والزجاج بيصير معتم |
| توفير بيانات أو 2G | الصورة الثابتة |
| ٤ أنوية أو ٤ غيغا رام أو أقل | الصورة الثابتة |
| التبويب مخفي | الفيديو بيوقف |
| قبل ما يحمّل الفيديو | الصورة الثابتة (ما في مستطيل أسود أبداً) |
| متصفّح ما بيدعم backdrop-filter | الألواح بتصير معتمة بدل شفافة |

الصورة الثابتة هي **الطبقة الأساسية**، والفيديو بينرسم فوقها — فحتى لو الفيديو ما وصل أبداً، التصميم بيضلّ صحيح.

## قرارات لازم تعرفها

**١. الزجاج ع الترويسة والفوتر ولوح واحد بكل صفحة — مش ع الكروت.** `backdrop-filter` بيجبر المتصفّح يعيد تضبيب اللي وراه بكل فريم من الفيديو. أربعين كرت مضبّب فوق فيديو شغّال = تقطيع ع أي لابتوب.

**٢. الداشبورد ضلّ معتم.** أداة شغل بدّها تباين، مش زجاج فوق فيديو. لو بدّك الزجاج هونيك كمان قلّي.

**٣. لمّا الفيديو يشتغل، الهيرو بيوقف يرسم صورته وسكريمه** — بيصير لوح زجاجي فوق الفيديو مباشرة. الكولاج الطايف بينطفي بهالوضع، لأنّه فوق فيديو بيصير زحمة.


---

# التعديلات ع ملاحظاتك (الصفحة الرئيسية)

## ١. صورة Against All Odds — راحت

**السبب:** الهيرو كان ياخد أفيش **أول عمل** بالكتالوغ ويحطّه خلفية ملء الشاشة:

```
backdropUrl={settings.hero_backdrop_url ?? posters[0]?.posterUrl ?? null}
                                          ^^^^^^^^^^^^^^^^^^^^^^^ هون المشكلة
```

شلت الـ fallback كلّه. هلق الخلفية بتيجي **بس** من الحقل اللي انت بتحطّه بالداشبورد — ما عاد في أي عمل بيقدر يصير خلفية بالغلط.

## ٢. الصفحة الرئيسية صارت متل كودك

لمّا الفيديو مفعّل، الهيرو صار `GlassHero` — نسخة طبق الأصل من الـ HTML اللي بعتّه:

- الفيديو شغّال لحاله بالخلفية، ما في ولا صورة فوقه
- لوح زجاجي واحد ع اليسار فيه النصّ
- زرّ **٧١** الدائري الزجاجي جوّا اللوح — بتكبسه بيفتح الفيلم ملء الشاشة مع صوت، و**رجوع للموقع** بيرجّعك

شلت من الهيرو: صورة الخلفية، السكريم، وخطوط الشبكة. الفيلم لحاله، متل ما بكودك بالزبط.

**الكولاج انتقل لقسم مستقلّ تحت الهيرو** — كروت طايفة فوق الفيديو بالهيرو نفسه بتتزاحم معه.

## ٣. حرية اختيار الكروت

لقيت إنّه ما كان عندك تحكّم فعلي: الهيرو كان بياخد **أول ٢٤ عمل** من الكتالوغ، مش المختارين.

هلق بـ `/admin/appearance` فوق في **"أعمال واجهة الرئيسية"**: شبكة أفيشات، بتكبس ع اللي بدّك — المختار بإطار أخضر وصح، وغير المختار باهت. بيحفظ فوراً بدون زرّ.

## ٤. الموقع صار زجاجي ليبيّن الفيديو

المشكلة كانت إنّ كل قسم بالصفحة عنده خلفية معتمة — فحتى لو الفيديو شغّال، ما بيبان.

عملت **`film-mode`**: كلاس واحد ع الحاوية الرئيسية، و CSS بيتكفّل بالباقي:

| العنصر | بدون فيديو | مع الفيديو |
|---|---|---|
| الأقسام | خلفية معتمة | شفافة |
| شريط الإحصائيات والشرائط | `#0f0f0f` معتم | زجاج مضبّب |
| الكروت | `bg-card` معتم | أسود ٤٠٪ — الأفيش بيضلّ واضح والفيديو بيبان وراه |
| العناوين | عادية | مع ظلّ نصّ مشان تبقى مقروءة فوق الفيديو |

قاعدة CSS وحدة بدل `glass ? ... : ...` بأربعين ملف — يعني أي قسم بتضيفه بعدين بياخد نفس المعاملة لحاله بدون ما أعدّله.
