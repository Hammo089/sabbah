# تحديث — نسخة كاملة

## ١. استبدل مجلد المشروع
احذف محتوى `~/Downloads/sabbah-platform` (ما عدا `.git` و `.env.local`) وفك الضغط مكانه.

## ٢. طبّق SQL بالترتيب
Supabase → SQL Editor:

1. `0011_modules_and_season_details.sql` — إذا ما طبّقته
2. `0012_backdrop_and_glass.sql` — إذا ما طبّقته
3. `0013_hero_copy_and_brand.sql` ← **الجديد**

`0013` بيضيف: نص الواجهة قابل للتعديل ب٣ لغات، تشغيل/إيقاف الواجهة، روابط الشعار،
تصميم زر ٧١، ونسخة الموبايل من الفيديو. كمان بيعمل باكِت `brand` (قراءة عامة، كتابة للموظفين).
تشغيله مرتين ما بيمحي نص عدّلته — بيملّي الفاضي بس.

## ٣. بعد التطبيق — من `/admin/appearance`

| القسم | شو بتحط |
|---|---|
| **نص الواجهة** | مفتاح التشغيل + السطر العلوي/العنوان/الكلمات المميّزة/الوصف — تبويب لكل لغة |
| **شعار الشركة** | رابط الشعار الفاتح والداكن (أو ارفع الملف مباشرة) |
| **تصميم زر ٧١** | PNG شفاف — بيحلّ محل الرقم والحركة بتبقى |
| **الفيديو الخلفي** | رابط النسخة الكاملة + **نسخة الموبايل (MP4)** + صورة الغلاف + مفتاح "تشغيل ع الموبايل" |

### مهم — الموبايل
الهاتف بيشغّل **ملف MP4 فقط**. رابط يوتيوب ما بيشتغل تلقائياً جوّا الصفحة على iOS —
بيطلع لوح "اضغط للتشغيل" ميت. لهيك:

1. ارفع النسخة الخفيفة (720p، قصيرة، بلا صوت) ع Supabase
2. حطها ب حقل **نسخة الموبايل (MP4)**
3. شغّل مفتاح **تشغيل ع الموبايل**

بدون هالخطوات الهاتف بيعرض صورة الغلاف — وهيدا مقصود، مش عطل.

## ٤. الشعار
الشعار الأصلي مقصوص ومرفق ب `public/brand/`:

- `cap-mark.png` — رمز CA لوحده (الهيدر)
- `cap-lockup.png` — الرمز + الاسم، للخلفيات الفاتحة
- `cap-lockup-dark.png` — نفسه والاسم أبيض، للأسود وفوق الفيديو
- `cap-mark-ar-gold.png` — النسخة العربية الذهبية

هدول الافتراضي. إذا حطيت رابط ب الأدمن بيتقدّم عليهم — يعني تغيير الشعار صار إعداد مش نشر جديد.

## ٥. متغيّرات Vercel (النوع **Config** مش Secret)

| المفتاح | لشو | إلزامي |
|---|---|---|
| `ANTHROPIC_API_KEY` | تلخيص النصوص + الشات | لهالميزات |
| `AI_MODEL` | اسم الموديل بحسابك | اختياري |
| `B2B_ACCESS_SECRET` | توقيع كوكي دخول المشترين | **نعم** |
| `CRON_SECRET` | تذكير العقود (وبيصير احتياطي لـ B2B) | نعم |
| `SUBMISSION_IP_SALT` | تجزئة IP المقدِّم | مستحسن |
| `RESEND_API_KEY` + `LICENCE_ALERT_FROM` | إيميل تذكير العقود | اختياري |

## ٦. الرفع

```bash
cd ~/Downloads/sabbah-platform
npm install
npm run build
git add -A && git commit -m "hero copy, brand marks, mobile backdrop" && git push
```
