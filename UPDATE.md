# تحديث — نسخة كاملة

## ١. استبدل مجلد المشروع
احذف محتوى `~/Downloads/sabbah-platform` (ما عدا `.git` و `.env.local`) وفك الضغط مكانه.

## ٢. طبّق SQL الجديد
Supabase → SQL Editor → شغّل الملفين بالترتيب:

1. `supabase/migrations/0009_licensing_and_theme.sql` (إذا ما طبّقته بعد)
2. `supabase/migrations/0010_submissions_and_chrome.sql` ← **الجديد**

## ٣. متغيّرات Vercel (Settings → Environment Variables، النوع **Config** مش Secret)

| المفتاح | القيمة | لشو |
|---|---|---|
| `ANTHROPIC_API_KEY` | مفتاحك من console.anthropic.com | تلخيص النصوص + الشات |
| `AI_MODEL` | اسم الموديل المتاح بحسابك | اختياري، الافتراضي `claude-sonnet-4-5` |
| `CRON_SECRET` | أي نص عشوائي طويل | تذكير انتهاء العقود |
| `SUBMISSION_IP_SALT` | أي نص عشوائي طويل | تجزئة IP المقدِّم (خصوصية) |
| `RESEND_API_KEY` | مفتاح resend.com | اختياري — إيميل تذكير العقود |
| `LICENCE_ALERT_FROM` | `alerts@yourdomain.com` | مع RESEND فقط |

بدون `ANTHROPIC_API_KEY` الموقع بيشتغل عادي: التقديم بينحفظ والملف بينرفع، بس بدون تلخيص وبدون شات.

## ٤. أوامر الرفع

```bash
cd ~/Downloads/sabbah-platform
npm install
npm run build
git add -A
git commit -m "feat: script submissions with AI reading, public assistant, partners pages, mobile player, light mode"
git push
```

`npm install` ضروري هالمرّة — في مكتبتين جداد (`unpdf` لقراءة PDF و `mammoth` لقراءة Word).

---

# الجديد بهالنسخة

## صفحة تقديم الأعمال — `/submit`
- استمارة من ٣ مراحل: معلومات المقدِّم، تفاصيل العمل، رفع الملف بالسحب والإفلات
- الملف بينرفع **مباشرة من المتصفح** ع bucket خاص (`submissions`) — ما بيمرق بالسيرفر، فما في سقف حجم
- كل تقديم بياخد رقم مرجعي فوراً (مثال `CAP-26-1B5829`) وبيظهر للمقدِّم
- **السرّية**: `anon` ما بيقدر يقرأ الجدول أبداً — لا صفوفه ولا صفوف غيره. مجرّب على PostgreSQL حقيقي: `permission denied for table script_submissions`
- honeypot + rate limit (٥ تقديمات لكل IP بالعشر دقايق)
- IP بينحفظ كـ hash مملّح فقط، مش كعنوان

## القراءة الآلية
بعد ما يوصل التقديم، الذكاء الاصطناعي بيفتح الملف (PDF / Word / نص)، بيقراه، وبيكتب تقرير قراءة داخلي:
ملخّص ٦-١٠ جمل، النوع، التيمات، الجمهور المستهدف، أعمال مشابهة، أقوى نقطة، أكبر خطر، وعلامة من ١٠٠.
كل هذا بيصير **بعد** ما يوصل الردّ للمقدِّم، فالاستمارة ما بتعلّق أبداً.

## الشات — على صفحة التقديم وع كل الموقع
- بجانب الاستمارة، وكمان كفقاعة بأسفل كل صفحة
- بيعرف: كل الأعمال المنشورة، تاريخ الشركة، شريط الأخبار، الشركاء، وقاعدة أسئلة وأجوبة بتحرّرها انت
- **بيقرأ بمفتاح anon** — يعني RLS نفسها بتمنعه إنه يذكر عمل مسوّدة، أو رخصة، أو سعر، أو تقديم حدا تاني. مش مجرّد تعليمات بالبرومبت
- بيرفض يتظاهر إنه بيقدر يوافق على عقد أو يعطي وضع تقديم معيّن
- الأسئلة والأجوبة الجاهزة بجدول `assistant_knowledge` — بالثلاث لغات

## مشغّل الفيديو — انعاد بناؤه للموبايل
المشاكل اللي كانت:

| المشكلة | الحلّ |
|---|---|
| ما بيشتغل ع الآيفون | كان الـ `setTimeout` بيقطع سلسلة لمسة المستخدم. هلق المشغّل بينبنى فوراً والستارة مجرّد طبقة فوقه |
| الفيديو بيختفي فجأة | أي buffering أو سحب بالشريط بيبعت PAUSED. هلق الإيقاف لازم يكون مقصود: بعد ١.٥ ثانية تشغيل، وبيثبت ٦٥٠ ملّي ثانية |
| الأزرار مقصوصة | `100dvh` بدل `vh` + `env(safe-area-inset)` — شريط سفاري ما بعاد ياكل الارتفاع |
| الفيديو أكبر من الشاشة بالوضع الأفقي | تحديد على المحورين معاً، فما بيتقصّ عمرو |
| صفحة تحت بتضلّ تسكرول | `position:fixed` مع إرجاع مكان السكرول (`overflow:hidden` سفاري بتتجاهله) |
| صوت مقفول بدون تفسير | لو المتصفّح رفض الصوت، بيشتغل مكتوم مع زرّ إلغاء كتم واضح |
| شاشة سودا وما في شي | بعد ٩ ثواني بيظهر رابط يوتيوب مباشر |

## فيديو الذكرى كخلفية للموقع
شغّله من `/admin/appearance`. الحمايات مبنيّة جوّا:
ما بينحمّل أبداً عالموبايل أو التابلت، ولا مع تقليل الحركة، ولا مع توفير البيانات، ولا ع جهاز ذاكرته ٤ غيغا أو أقل، ولا ع معالج ٤ أنوية أو أقل. وبيتوقّف لحاله لما التبويب يختفي أو لما تنزل تحت أول شاشة. بتحدّد الشفافية والمكان (الرئيسية فقط / كل الصفحات).

## شاشة الانتقال بين الصفحات
شعار الصباح مع شريط ذهبي بيلفّ حواليه. ٤ أشكال: حلقة، شريط، نبض، أو مطفي. بتتحكّم بالمدّة وبترفع شعارك من `/admin/appearance`.

## الوضع النهاري — انصلح
كان في ٢٨ ملف فيهم ألوان مثبّتة (`#000000`، `text-neutral-400`، `border-white/10`). كلها انحوّلت لـ design tokens. والأقسام اللي فيها نص فوق صورة عندها كلاس `.on-media` بيعيد تعريف التوكنز محلياً — فبتضلّ مقروءة بالوضعين بدون ولا لون مثبّت. وألوان الداشبورد للـ primary/accent هلق بتنزل ع `:root` مش بس `.dark`.

## تداخل الصفحة الرئيسية — انصلح
شريط الأفيشات كان ياخد ٤٨٪ من العرض والنص `max-w-[680px]` — فيتداخلوا بين ١٠٢٤ و١٢٨٠ بكسل. هلق الشريط ٤٢٪ عند lg والنص محدود بـ ٥٢٪. وكمان تصغير التباعد والخط عالموبايل مشان ما يطلع برّا الشاشة.

## شريط الأخبار
السرعة صارت قابلة للتعديل (١٠ ل٢٤٠ ثانية للدورة) من مكانين: `/admin/ticker` مباشرة، ومن `/admin/appearance`. والافتراضي ٣٨ ثانية — كبّر الرقم يبطّئ.

## صفحات الشركاء
- `/partners` — كل محطة ومنصّة اشترت مننا، مع عدد الأعمال
- `/partners/[slug]` — صفحة كل شركة مع كل الأعمال اللي اشترتها

## صفحات جديدة كمان
`/submit`, `/partners`, `/partners/[slug]`, `/admin/submissions`

---

## الصور اللي وعدتني فيها
ابعتلي صورة مشكلة الفيديو عالموبايل — عدّلت المشغّل بناءً على المشاكل التقنية المعروفة اللي فوق، بس لو المشكلة عندك شي تاني (مثلاً الحجم بصفحة معيّنة) الصورة بتخلّيني أصلّحها بالزبط.
